---
title: Markdown
description: 一种轻量级标记语言，使用纯文本格式编写结构化文档
tags: [写作, 格式化, 标记语言]
updated: 2024-01-01
---

## 什么是 Markdown？

Markdown 是一种轻量级标记语言，由 John Gruber 于 2004 年创建。它允许你使用易于阅读和书写的纯文本格式，然后将其转换成结构化的 HTML 文档。

Markdown 的设计哲学是"易读易写"，即便是未经渲染的纯文本也应该清晰可读。

## 基本语法

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 强调

```markdown
**粗体文字**
*斜体文字*
~~删除线~~
```

### 列表

```markdown
- 无序列表项
- 另一项

1. 有序列表
2. 第二项
```

### 链接与图片

```markdown
[链接文字](https://example.com)
![图片描述](image.png)
```

## 扩展语法

现代 Markdown 处理器通常支持扩展语法，例如：

- **表格** — 使用 `|` 分隔列
- **代码块** — 使用三个反引号包围代码
- **脚注** — 使用 `[^1]` 语法
- **数学公式** — 使用 `$` 包围 LaTeX 表达式

## 相关知识

- [[w/static-site-generator|静态站点生成器]] — 通常使用 Markdown 作为内容格式
